@model ShopOrder
@{
    Layout = null;
}

@*<section class="card">
    <div class="card-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h3 class="card-title"><i class="fab fa-windows"></i> @ViewBag.Title</h3>
    </div>
    <div class="card-body">
        <partial name="_PartialCharge" />
        <div class="mt-md">
            @if (ViewBag.CashAddInfo != null)
            {
                 C info = (CashAddInfo)ViewBag.CashAddInfo;
                <div class="alert alert-info">
                    <h4 class="text-center">Trạng thái thẻ phía Đối tác</h4>
                    <div class="row">
                        <label class="col-sm-4">Trạng thái:</label>
                        <div class="col-sm-8">@Html.EnumDisplayFor(m => info.Status)</div>
                        <label class="col-sm-4">Mã lỗi:</label>
                        <div class="col-sm-8">@Html.DisplayFor(m => info.ResCode) (@Html.DisplayFor(m => info.ErrorText))</div>
                        <label class="col-sm-4">Mệnh giá:</label>
                        <div class="col-sm-8">@Html.DisplayFor(m => info.Amount)</div>
                    </div>
                </div>
            }
        </div>
    </div>
    <div class="card-footer text-right">
        @if (Model.Status == CashStatus.Pending || (User.Identity.Name == "Admin" && Model.Status != CashStatus.Success))
        {
            <a class="btn btn-info modal-links" href="@Url.Action("ChargeApprove", new { id = Model.Id })"><i class="fas fa-check"></i> Duyệt nạp thẻ</a>
        }
        <button class="btn btn-warning" data-dismiss="modal"><i class="fas fa-undo"></i> Đóng lại</button>
    </div>
</section>*@